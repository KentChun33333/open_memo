# 🗂 Node modules – don’t commit them
node_modules/

# ⚙️ Next.js build output (the `.next` folder is created at runtime)
.next/
.out/
.build/
.dist/
/.vercel/
/.vercel_build_output/
/vercel.json

# 📦 Cache folders generated by package managers / tooling
.npm/
.yarn/
.pnpm/
/.pnpm-debug.log
/.pnpm-lazy-resolver/

# 🔧 Environment files (keep local secrets out of the repo)
.env.local
.env.development
.env.test
.env.production

# ⚡️ Debug / log files generated by npm/yarn/pnpm
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnpm-debug.log*

# 🗂 IDE / editor artefacts (optional – add only what you use)
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*~ 

# 📁 Temporary files created by Node & Vite
*.cache/
.cache-loader/

# 🎨 Tailwind / PostCSS generated CSS (if you ever generate it manually)
css/*.css
postcss.config.*
tailwind.config.cjs

# .DS_Store and Thumbs.db are OS-level metadata we don’t need in git
.DS_Store
Thumbs.b

# 📁 Other generated configuration files that can be recreated
package-lock.json          # if you use npm (keep it optional – remove if you prefer lockfiles)
yarn.lock                  # for Yarn users
pnpm-debug.log             # keep the debug log out of git

# 📦 Utility / Stubs folders that are not needed at runtime
node_modules/tailwindcss/stubs/
node_modules/react/**/cjs/   # optional: you usually only need ESM builds in prod